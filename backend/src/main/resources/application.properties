# ============================================
# Axe Breaker API - Application Properties
# ============================================

server.port=8082

# -------------------------
# MySQL Database
# -------------------------
spring.datasource.url=jdbc:mysql://localhost:3306/X_breaher
spring.datasource.username=root
spring.datasource.password=Jatin@12
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# -------------------------
# JPA / Hibernate
# -------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Hibernate detects MySQL automatically
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# -------------------------
# Jackson
# -------------------------
spring.jackson.serialization.fail-on-empty-beans=false

# -------------------------
# Logging
# -------------------------
logging.level.com.X_axe_breaker=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# -------------------------
# Company Email
# -------------------------
company.email=axebreaker0001@gmail.com

# -------------------------
# Gmail SMTP
# -------------------------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=axebreakerofficial@gmail.com
spring.mail.password=ynsz lijv ntxf wbyi
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain,application/javascript,text/css
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.default-encoding=UTF-8
# ==========================================
# AXE BREAKER ADMIN LOGIN
# ==========================================

axe.admin.username=axebreaker
axe.admin.password=axebreaker@123

# ==========================================
# ADMIN SESSION COOKIE
# ==========================================

server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=strict
server.servlet.session.cookie.secure=${SESSION_COOKIE_SECURE:false}

# 8 hour server-side session
server.servlet.session.timeout=8h